import SwiftUI

/// The app's single accent color (ТЗ 4.1: "один акцентный цвет на весь
/// интерфейс"). Change `hue` here to re-theme the whole app.
enum AppTheme {
    static let accent = Color.oklch(l: 0.78, c: 0.17, h: 145)
}
